#!/usr/bin/env bash

  if [ -f .gitignore ]; then
        while IFS= read -r pattern; do
            echo "Removing files with matching pattern: $pattern"
            git ls-files --cached --ignored --exclude-standard | grep "$pattern" | xargs -I {} rm -f {}
        done < .gitignore
    fi

