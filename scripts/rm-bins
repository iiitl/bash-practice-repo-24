#!/usr/bin/env bash

# find all the files in current directory
for files in ./*; do
    # check that file is binary or not
    if [[ $(files "$files") == *"executable"* ]]; then
        #remove all executable file
        rm "$file"
    fi
done