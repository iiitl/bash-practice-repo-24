#!/bin/bash
generate_password() {
    local length=$1
    local password=$(openssl rand -base64 1000 | tr -dc 'a-zA-Z0-9!@#$%^&*()_+' | head -c "$length")
    echo "$password"
}
read -p "Enter the length of the password: " length
generate_password $length